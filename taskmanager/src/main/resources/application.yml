spring:
  datasource:
    url: jdbc:mysql://localhost:3306/taskmanager_db
    username: root
    password:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
